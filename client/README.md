# Varun's Chrip APP

Chirps is a Social networking app build on Aurelia Framework

  ```
3. Ensure that [Gulp](http://gulpjs.com/) is installed. 

  ```shell
  npm install -g gulp

6. To run the app, execute the following command:

  ```shell
  gulp watch
  ```

